import React, { Fragment, useState } from 'react';

function JoinSession({ setSessionId }) {
    return (
        <Fragment>
            <button className="btn btn-light">Join existing session</button>
        </Fragment>
    );
}

export default JoinSession;